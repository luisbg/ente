ENTE (Educational Nimble Text Editor)
----------------
       _
    __( Ꙩ<
    \___)

A text editor project to keep learning Rust through practice.

It edits files now. Check out the Actions list in keys.conf.

Comments and patches welcome :)

To run:
$ cargo run text_file
or
$ cargo run text_file --keyconfig=<config_file>  --hide-line-num
